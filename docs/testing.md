# Testing

Install dependencies from `requirements.txt`, activate a virtual environment, and configure local credentials using `.env`.

The `tests/` directory contains conversation recovery tests, complex-dialogue tests, spreadsheet checks, voice-quality checks, and chaotic-call simulations.

Some checks call paid external services or require a configured LiveKit/SIP environment. Run those only in a controlled development account with test numbers and consent.
